# 本机开发用的极简 Redis 配置
# 正式压测请用 docker-compose.yml 里的 Redis，参数与线上一致

port 6379
bind 127.0.0.1
requirepass redis123456

# 开发环境不需要持久化，关掉省 IO；压测时也避免 RDB 落盘干扰耗时统计
save ""
appendonly no

maxmemory 256mb
maxmemory-policy noeviction

# Windows 移植版没有 daemonize，靠 Start-Process 后台启动
logfile "redis.log"
