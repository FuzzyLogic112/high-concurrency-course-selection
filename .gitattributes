# 统一按 LF 存库，检出时由各平台自行处理。
# 不加这个的话，Windows 上 git add 会刷一屏 CRLF 警告，
# 而且别人在 Linux 上 clone 出来的 .sh / mvn 脚本会带 ^M 跑不起来。
* text=auto eol=lf

# 二进制，绝不做行尾转换
*.png binary
*.jar binary
*.exe binary
*.dll binary
*.pdb binary
*.cmd text eol=crlf
*.bat text eol=crlf
